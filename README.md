# portfolio

Static portfolio site: WebGL image spiral, list view, about page, project pages and a small synth sound layer.

- Live: GitHub Pages (root of this repo)
- Edit content (name, bio, projects, links, images): the source project (kept alongside the Instinct File); this repo holds the built site

All images are generated placeholders and the text is placeholder copy.
